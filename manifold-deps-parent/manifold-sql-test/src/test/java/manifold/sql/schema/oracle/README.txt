Oracle requires a server, so these tests are saved as *.java_. To build and run them rename files to remove the trailing
'_' and execute the corresponding Postgres DDL files in resources. I use IntelliJ Ultimate database tools for this.

At some point I'll build containers for these types of tests.