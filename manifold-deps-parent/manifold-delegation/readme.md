# manifold-delegation

> [!IMPORTANT]
> **Renamed to [manifold-parts](../manifold-parts).** This directory remains only to
> keep existing links working; all development continues there.

## Migrating from manifold-delegation

- **Artifacts:** `manifold-delegation*` → `manifold-parts*` (e.g. `manifold-delegation-rt` → `manifold-parts-rt`).
- **Imports:** package `manifold.ext.delegation` → `manifold.ext.parts`.
- `@link`, `@part`, and their semantics are unchanged.

Full documentation: **[manifold-parts](../manifold-parts)**.
